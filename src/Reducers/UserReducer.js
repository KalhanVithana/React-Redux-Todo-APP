import { Register_error, Register_Load, Register_Sucess } from "../constant/UserConstant"

const initialstate = {
    image:'add'
}

export const RegisterUser = (state= initialstate,action)=>{

    switch(action.type){
       

             default :
            return state;
    }
}